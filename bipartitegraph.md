Bipartite Graph:
-> A bipartite graph is a graph whose vertices can be divided into two groups (sets) such that:
    No two vertices within the same group are directly connected.
-> Linear Graph with no cycle are always bipartite.
-> Graph with even cycle length can also be bipartite;
